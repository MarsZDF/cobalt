name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust: [stable, beta, nightly]
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust (${{ matrix.rust }})
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}
      
      - name: Cache cargo registry
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ matrix.rust }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-${{ matrix.rust }}-
      
      - name: Test cobol-lexer
        working-directory: cobol-lexer
        run: cargo test --verbose
      
      - name: Test cobol-ast
        working-directory: cobol-ast
        run: cargo test --verbose
      
      - name: Test cobol-parser
        working-directory: cobol-parser
        run: cargo test --verbose
      
      - name: Test cobol-migration-analyzer
        working-directory: cobol-migration-analyzer
        run: cargo test --verbose
      
      - name: Test cobol-doc-gen
        working-directory: cobol-doc-gen
        run: cargo test --verbose
      
      - name: Test cobol-repl
        working-directory: cobol-repl
        run: cargo test --verbose || echo "No tests yet"
      
      - name: Test cobol-linter
        working-directory: cobol-linter
        run: cargo test --verbose || echo "No tests yet"
      
      - name: Test cobol-visualizer
        working-directory: cobol-visualizer
        run: cargo test --verbose || echo "No tests yet"

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      
      - name: Cache cargo registry
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-lint-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-lint-
      
      - name: Check formatting (cobol-lexer)
        working-directory: cobol-lexer
        run: cargo fmt -- --check
      
      - name: Check formatting (cobol-ast)
        working-directory: cobol-ast
        run: cargo fmt -- --check
      
      - name: Clippy (cobol-lexer)
        working-directory: cobol-lexer
        run: cargo clippy -- -D warnings
      
      - name: Clippy (cobol-ast)
        working-directory: cobol-ast
        run: cargo clippy -- -D warnings
      
      - name: Check formatting (cobol-parser)
        working-directory: cobol-parser
        run: cargo fmt -- --check
      
      - name: Clippy (cobol-parser)
        working-directory: cobol-parser
        run: cargo clippy -- -D warnings -A clippy::needless_borrow -A clippy::clone_on_copy
      
      - name: Check formatting (cobol-migration-analyzer)
        working-directory: cobol-migration-analyzer
        run: cargo fmt -- --check
      
      - name: Clippy (cobol-migration-analyzer)
        working-directory: cobol-migration-analyzer
        run: cargo clippy -- -D warnings
      
      - name: Check formatting (cobol-doc-gen)
        working-directory: cobol-doc-gen
        run: cargo fmt -- --check
      
      - name: Clippy (cobol-doc-gen)
        working-directory: cobol-doc-gen
        run: cargo clippy -- -D warnings

  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        rust: [stable]
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust (${{ matrix.rust }})
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}
      
      - name: Cache cargo registry
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-build-${{ matrix.rust }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-build-${{ matrix.rust }}-
      
      - name: Build cobol-lexer
        working-directory: cobol-lexer
        run: cargo build --verbose --release
      
      - name: Build cobol-ast
        working-directory: cobol-ast
        run: cargo build --verbose --release
      
      - name: Build cobol-parser
        working-directory: cobol-parser
        run: cargo build --verbose --release
      
      - name: Build cobol-migration-analyzer
        working-directory: cobol-migration-analyzer
        run: cargo build --verbose --release
      
      - name: Build cobol-doc-gen
        working-directory: cobol-doc-gen
        run: cargo build --verbose
      
      - name: Build cobol-repl
        working-directory: cobol-repl
        run: cargo build --verbose
      
      - name: Build cobol-linter
        working-directory: cobol-linter
        run: cargo build --verbose
      
      - name: Build cobol-visualizer
        working-directory: cobol-visualizer
        run: cargo build --verbose --release

  examples:
    name: Build Examples
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Cache cargo registry
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-examples-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-examples-
      
      - name: Build examples (cobol-lexer)
        working-directory: cobol-lexer
        run: cargo build --examples --verbose
      
      - name: Build examples (cobol-parser)
        working-directory: cobol-parser
        run: cargo build --examples --verbose

  docs:
    name: Build Documentation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Cache cargo registry
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-docs-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-docs-
      
      - name: Build docs (cobol-lexer)
        working-directory: cobol-lexer
        run: cargo doc --no-deps --verbose
      
      - name: Build docs (cobol-ast)
        working-directory: cobol-ast
        run: cargo doc --no-deps --verbose
      
      - name: Build docs (cobol-parser)
        working-directory: cobol-parser
        run: cargo doc --no-deps --verbose
      
      - name: Build docs (cobol-migration-analyzer)
        working-directory: cobol-migration-analyzer
        run: cargo doc --no-deps --verbose
      
      - name: Build docs (cobol-doc-gen)
        working-directory: cobol-doc-gen
        run: cargo doc --no-deps --verbose
